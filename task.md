1. 我们先来一个热身任务，根据Pleiades设计文档，先实现运行时层
完成

2. 我要求你在Test目录下完成下列这些任务
    - 在Test/Model目录下编写一个CNN模型代码，随机初始化其参数，然后将模型权重保存到Test/Weights中
    - 在Test/Model目录下编写一个ViT模型代码，随机初始化其参数，然后将模型权重保存到Test/Weights中
    - 在Test目录下编写一个总的Test.py文件，这个文件将会依据输入的命令行参数调用其他测试脚本中的测试用例进行测试
    - 在Test目录下编写一个Runtime_Test.py文件，这个文件提供一系列测试用例，测试运行时层中的所有方法，在测试模型运行相关时，使用Model目录下的所有模型进行测试
当前的测试脚本写的有些问题，Runtime_Test.py不需要写的那么复杂，重新修改测试脚本
增加系统快照测试函数输出网络信息的语句
完成

3. 修改运行层的实现，网络信息要返回带宽，延迟等信息，然后在测试中也要加上相应的输出内容。最后在Test目录下增加一个说明文档test.md说明Test目录用于做什么，以及怎么用
完成

4. 完成以下任务
   - 根据设计文档，完成配置组件
   - 根据设计文档，完成日志组件
   - 根据设计文档，更新Runtime组件实现
完成

5. 完成以下任务
   - 根据据设计文档，更新配置组件
   - 根据设计文档，完成通信层组件