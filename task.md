1. 下面我们需要先弄明白rust版本的libp2p应该如何使用。我们需要使用libp2p完成下面这些事情：首先，我们需要用libp2p实现互相发现，组网的能力，然后需要使用libp2p当中的DHT功能，然后需要建立连接，在设备之间传输命令，以及传输文件。如果要实现这些功能，我们需要libp2p库的哪些功能呢？在docs目录下写一个Rust_Libp2p.md文档，说明需要使用的功能，以及应该如何使用。这里是rust libp2p的网页文档https://docs.rs/libp2p/latest/libp2p/，如果有需要的话就查阅此文档。
   完成
2. 下面我们再来弄明白rust的candle库是怎么为神经网络提供runtime的。对于runtime，我们的要求很简单，通过p2p将模型权重下载到本地，然后通过命令，将输入之类的传递过来，然后加载模型权重并且运行即可。同时，我们可能还需要将模型进行切分，每台设备可能只运行模型的某几层。在docs目录下写一个Rust_Candle.md文档，说明我们需要使用哪些功能以及如何使用，并且讨论runtime要求的模型格式，以及模型如何进行切分。
   完成
3. 经过讨论，我们决定使用onnx runtime为神经网络提供runtime。对于runtime，我们的要求很简单，通过p2p将模型权重下载到本地，然后通过命令，将输入之类的传递过来，然后加载模型权重并且运行即可。同时，我们可能还需要将模型进行切分，每台设备可能只运行模型的某几层。在docs目录下写一个Rust_Onnx.md文档，说明我们需要使用哪些功能以及如何使用，并且讨论runtime要求的模型格式，以及模型如何进行切分。
   完成
4. 现在我们来考虑一下管理层，显然管理层的主要功能是收发命令，根据命令来执行一些操作，它可以被动的在后台挂起，等到有命令进来时，它可以根据命令执行操作。也可以主动的接收用户的指令，然后根据某些规则，选择p2p网络中的一些节点，将用户的指令转换成一些具体的命令发送给这些设备，与这些设备协同执行任务。在docs目录下写一个Rust_Manager.md文档，说明我们是否需要一些外部的库来实现这些功能或者说我们需要完全用rust自行实现。
   完成
5. 除了这些基本功能的模块外，我们还需要一个日志功能，rust应当如何实现这样一个日志模块，在docs目录下写一个Rust_log.md文档，说明我们如何实现一个日志模块
   完成
6. 根据C:\workspace\Workspace\Workspace\Pleiades\docs\Rust_log.md文件中的内容，我们不需要自行实现一套log系统，而是采用rust已有的log系统，先修改设计文档markdown文件当中日志组件的内容，先简要说明rust的log系统，然后为属性，方法section补充rust log系统的内容
   完成
7. 根据设计文档里面配置组件section的内容，创建文件并写入对应内容。
完成
8. 根据当前main.rs当中的注释，不全代码，同时修改cargo.toml文件补全对应的依赖库。只根据我当前的配置文件编写代码即可，不要添加额外的配置上去
 完成
9. 根据设计文档当中，网络层部分的相关内容，实现网络层的对应代码。  