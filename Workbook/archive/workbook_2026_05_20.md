# 2026-05-20

## git pull + 分支确认 (completed)
开始: 2026-05-20
- git stash .zgent/terminal-output/term-1.bin → git pull origin reforge
- 3ef48eb → c3bac12 (清理 .zgent 临时文件)
- 添加 .gitignore: .zgent/ + uploads/

## ls 不显示层信息 Bug 修复 (completed)
开始: 2026-05-20
根因: GGUF_Analyze_And_Convert (gguf_model_manager.rs:567)
- Build_Layer_Bitmap 中 layer_tensors_map.entry(layer_idx).or_default() 只创建空Vec
- Build_Layer_Bitmap 检查 !tensors.is_empty() → 永远false → 全零bitmap
- TUI bitmap_hex_to_layer_range: layers.is_empty() → 不显示

修复: Build_Layer_Bitmap 改为 contains_key(&i) 替代 get+tensors.is_empty()
- 文件: Src/ML_Engine/gguf_model_manager.rs:495-512
- cargo check --no-default-features: 0 errors, 19 warnings(pre-existing)

## PeerManager Code Review + 修复 (completed)
开始: 2026-05-20
修改文件:
- Src/PeerManagement/peer_manager.rs (去内层Arc, upsert返回bool+保留旧字段, get_local_peer新增, get_peers/get_all_peers 剥离layer_time)
- Src/PeerManagement/capability.rs (Upsert_Peer→Result, 删Timeout, 增Get_All_Peers+Get_Local_Peer)
- Src/PeerManagement/peer_handle.rs (实现新trait方法)
- Src/Orchestrator/mod.rs (StubPeerManager委托PeerManager, inject_peer注入mock)
- Src/Network/swarm_events.rs (Upsert_Peer→.ok())
cargo check --no-default-features: 0 errors, 19 pre-existing warnings

## peer name 功能实现 (completed)
开始: 2026-05-20
文件变更:
- Src/Config/config.rs: +Identity_Config, +Get_Peer_Name, +Set_Peer_Name
- Src/Config/config.toml: +[Identity] peer_name = "new_peer"
- Src/Config/mod.rs: 导出 Get_Peer_Name/Set_Peer_Name/Identity_Config
- Src/PeerManagement/peer_info.rs: PeerInfo +name, +display_name(), +set_name(), new_local()加name参数
- Src/PeerManagement/peer_manager.rs: new()加name, +get_peer_by_name, +set_local_name
- Src/PeerManagement/capability.rs: +Get_Peer_By_Name, +Set_Local_Name
- Src/PeerManagement/peer_handle.rs: 实现新trait方法
- Src/PeerManagement/mod.rs: create_peer_management加name参数
- Src/Orchestrator/mod.rs: StubPeerManager适配
- Src/Orchestrator/command.rs: +SetName{name}
- Src/main.rs: Get_Peer_Name→create_peer_management
- Src/TUI/mod.rs: set-name命令解析
- Src/Orchestrator/core/branch_user.rs: SetName处理+持久化, dp改用display_name(), +Path import

规则: display_name() = name#XXXX(前4位peer_id) 或 "unknown"
set-name alice → 内存:PeerManager.set_local_name + 磁盘:config.toml持久化
cargo check --no-default-features: 0 errors

## Network Info 单向推送 (completed)
开始: 2026-05-20
连接建立 → 自动发送本地 Info (name + supported_models JSON) → 对方解析更新 PeerManager

变更:
- Src/PeerManagement/peer_info.rs: SupportedModel +Serialize +Deserialize (layer_bitmap hex)
- Src/PeerManagement/capability.rs: +Update_Peer_Name
- Src/PeerManagement/peer_manager.rs: +update_peer_name
- Src/PeerManagement/peer_handle.rs: impl Update_Peer_Name
- Src/Orchestrator/mod.rs: StubPeerManager +Update_Peer_Name
- Src/Network/swarm_events.rs: ConnectionEstablished → Get_Local_Peer → send_request(Info, "name|json"); DataType::Info 分支解析+更新

Payload 格式: "name|[{id,file_name,layer_bitmap_hex},...]"
单向, 回复 OK 忽略
cargo check --no-default-features: 0 errors
