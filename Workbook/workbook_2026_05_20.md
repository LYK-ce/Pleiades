# 2026-05-20

## git pull + 分支确认 (completed)
开始: 2026-05-20
- git stash .zgent/terminal-output/term-1.bin → git pull origin reforge
- 3ef48eb → c3bac12 (清理 .zgent 临时文件)
- 添加 .gitignore: .zgent/ + uploads/

## ls 不显示层信息 Bug 修复 (completed)
开始: 2026-05-20
根因: GGUF_Analyze_And_Convert (gguf_model_manager.rs:567)
- Build_Layer_Bitmap 中 layer_tensors_map.entry(layer_idx).or_default() 只创建空Vec
- Build_Layer_Bitmap 检查 !tensors.is_empty() → 永远false → 全零bitmap
- TUI bitmap_hex_to_layer_range: layers.is_empty() → 不显示

修复: Build_Layer_Bitmap 改为 contains_key(&i) 替代 get+tensors.is_empty()
- 文件: Src/ML_Engine/gguf_model_manager.rs:495-512
- cargo check --no-default-features: 0 errors, 19 warnings(pre-existing)

## PeerManager Code Review + 修复 (completed)
开始: 2026-05-20
修改文件:
- Src/PeerManagement/peer_manager.rs (去内层Arc, upsert返回bool+保留旧字段, get_local_peer新增, get_peers/get_all_peers 剥离layer_time)
- Src/PeerManagement/capability.rs (Upsert_Peer→Result, 删Timeout, 增Get_All_Peers+Get_Local_Peer)
- Src/PeerManagement/peer_handle.rs (实现新trait方法)
- Src/Orchestrator/mod.rs (StubPeerManager委托PeerManager, inject_peer注入mock)
- Src/Network/swarm_events.rs (Upsert_Peer→.ok())
cargo check --no-default-features: 0 errors, 19 pre-existing warnings
