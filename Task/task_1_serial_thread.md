# Task 1: Serial Thread

> 状态：待开始
> 创建日期：2026-07-07

## 目标

重构 Robot 底层串口通信，建立独立的多串口线程模型。

## 背景

当前 Robot 的串口通信耦合在 `SerialIo` 中，单串口、单线程，
无法扩展到多设备（STM32 控制板、LiDAR 雷达、摄像头等）场景。

## 范围

- 底层：通用串口线程抽象（打开、收发、关闭）
- 每设备一个独立线程，互不干扰
- 线程生命周期管理（启动、优雅退出）

## 人类评审

<!-- 在此区域写下评审意见 -->

