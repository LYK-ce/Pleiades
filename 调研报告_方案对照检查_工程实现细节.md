# 多车地图一致性方案 —— 工程实现细节调研 + 方案对照检查

> 任务：`Task/task_13_2_crdt_map.md`（定案）+ `Workbook/wb_13_2_crdt_map.md`（收敛链）
> 前置调研：`调研报告_分布式地图一致性方案.md`（本次在**其未覆盖的工程实现细节**上补强，并对照检查我们定案的方案）
> 调研方式：web 检索 + 直接读开源源码（m-explore map_merge、rust-libp2p gossipsub 0.49.5 源码、OrbitDB sync 协议、Filecoin ChainSync 规范、gossipsub v1.0/v1.1 规范）+ 读本项目代码（`Src/Network/Gossipsub/mod.rs`、`Src/Network/swarm_events.rs`、`Src/Network/network_service.rs`、`Src/Network/command_handler.rs`、`Src/Robot/slam/grid.rs`、`Src/Robot/core/protocol/messages.rs`、`frame.rs`）
> 结论速览：**方案有条件成立**。核心骨架（增量广播 + 全量初始化 + 有界截断）在工程上有充分先例；但对照检查发现 **3 个实施前必须修正的点**（gossipsub 64KB 消息上限、full/delta 顺序与去重语义、无 anti-entropy 与带宽估算的不自洽），另有若干中低严重度问题见 §3。

---

## 0. 本次调研补充了什么（上一轮报告没有的）

| # | 上一轮报告缺 / 弱 | 本次补充（见 §1） |
|---|---|---|
| 1 | m-explore map_merge 只写了"log-odds 空间合并" | 读了**源码**：现行 master 实际用 `cv::max` 逐格取 max（占优），不是 log-odds 加法；且它是**"全量 map topic + 可选增量 map_updates topic"双 topic 结构**（与我们 FULL/DELTA 分 topic 同构） |
| 2 | OctoMap clamp 只写了"带 clamp 阈值" | 具体参数：默认 clamp lmin=-2 / lmax=3.5（概率 0.12/0.97）；多机合并 = 重叠体素按 log-odds 方程当新观测加和 |
| 3 | gossipsub 快照缓存只说"类似 MQTT retained" | 确认 **libp2p gossipsub 无原生 retained/LVC**；`libp2p/research-pubsub#12` 正是"新订阅者→发布者重播最后一条"的设计提案（源自 OrbitDB 加群初始化问题）；我们 SnapshotCache 就是该模式 |
| 4 | 新节点初始化没给工程先例 | 补全：**OrbitDB**（订阅 topic 后 peer 反向 dial 直接交换 log heads，再 pubsub 增量）、**Filecoin ChainSync/BlockSync**（gossip 只做实时传播、状态同步走 pull）、**Raft InstallSnapshot**、**Ethereum snap sync**、**Fabric state transfer**、**ZMQ Clone/LVC**（并明确指出其快照-增量竞争坑）、**DDS TRANSIENT_LOCAL / ROS1 latched**（broker/DDS 侧订阅即得最后样本） |
| 5 | "有界 + 无 anti-entropy"没有工程裁决 | 找到决定性证据：**同类收敛证明都依赖"单调 + 幂等"**（max-consensus、stigmergic 有界单调计数器），**没有"加减双向 + 无反熵"的收敛先例**；无 anti-entropy 的加法计数器丢包缺口是永久的，除非饱和+持续观测修复 |
| 6 | 消息 ID 去重"内容哈希" | 读 rust-libp2p 0.49.5 源码：**默认消息 ID = (source peer_id, seqno)，不是内容哈希**；且**默认 max_transmit_size = 65536B**——MAP_FULL 帧（65556B payload + ~53B 帧头）会超限发布失败 |
| 7 | 带宽估算 400 格/周期 | 指出与"含饱和格扫到就发"（wb 决策4）不自洽：视野内可观测格 ~1.1 万，若饱和格也发，带宽放大 10~30 倍 |

---

## 1. 调研补充（工程实现细节，带链接）

### 1.1 occupancy grid 多机共享的具体实现

#### 1.1.1 m-explore / map_merge（ROS1 + ROS2）—— 读源码结论

- 仓库：`hrnr/m-explore`（ROS1）https://github.com/hrnr/m-explore ；`robo-friends/m-explore-ros2`（ROS2）https://github.com/robo-friends/m-explore-ros2
- **架构**：集中式 merge 节点。每车发布 `<ns>/map`（全量 `nav_msgs/OccupancyGrid`），可选发布 `<ns>/map_updates`（增量 `OccupancyGridUpdate`）。merge 节点订阅所有车 → 用估计的相对变换把所有栅格 warp 到公共帧 → 融合 → 发布 merged map。
- **融合规则（关键，与上一轮报告说法不同）**：`map_merge/src/combine_grids/grid_compositor.cpp` 现行 master 用 OpenCV：
  ```cpp
  cv::Mat result(dst_roi.size(), CV_8S, ...);          // 有符号 i8，-1=未知
  ...
  cv::max(result_roi, warped_signed, result_roi);      // 重叠格逐格取 max
  ```
  即：重叠区域**取占优值（max of occupancy 0..100）**，不是 log-odds 加法。（log-odds 加法是 Birk & Carpin 2006 的理论做法；m-explore 工程实现走了更简单的 max 融合。）
- **增量 topic 的作用**（wiki_doc.txt 原文要点）：`map_updates` 可选；**若 mapping 算法不提供增量，则 merged map 只能在全量 map 更新时刷新**——即"全量周期 = 对账兜底，增量 = 优化"，与我们 FULL/DELTA 分 topic 的设计同构，但注意**m-explore 里全量本来就是老节点的反熵来源**。
- 已知初始位姿 / 未知初始位姿（特征匹配）两种模式；未知模式要求地图有足够重叠，否则合并质量差。

#### 1.1.2 OctoMap 多机建图（JFR/IEEE 2014 + 工程）

- 论文：*Robust and efficient multi-robot 3D mapping with octree based occupancy grids*，IEEE 2014（https://ieeexplore.ieee.org/document/6974556 ；PDF https://www.researchgate.net/publication/290875273 ）
- **合并机制**：每车本地建 octree；合并时先把八叉树降为体素质心点云 → 变换到共同坐标系 → 重建体素 → **相交体素按 log-odds 方程"当作一次新观测"加和**（即 log-odds 加法 + clamp）。
- **clamp 参数（工程默认）**：OctoMap 默认 `clamping_thres_min=-2`、`clamping_thres_max=3.5`（对应概率 0.12 / 0.97）。注意**比我们的 ±8 小得多**。理由（Yguel 2007 *Update Policy of Dense Maps*, FSR 2007, https://inria.hal.science/inria-00199713/document ；OctoMap 论文 §3）：clamp 越紧 → 地图越"可更新"（动态环境可翻转），也压缩存储；越宽 → 越稳定但丢失动态性。我们 ±8/阈值 ±6 属于"宽 clamp"阵营：稳态强、动态翻转慢——用于"几辆矿卡共享静态地图"是合理选择，但要意识到**这不是向 OctoMap 对齐**，而是更保守的参数（比论文建议宽 2~4 倍）。
- **过度自信（重复计数）**：多车观测同一物体各自 +3，log-odds 相加违反独立性假设 → 过度自信。OctoMap 与 m-explore 都接受"饱和截断 + 阈值"作为折中；我们的 clamp 同样天然限制上界。

#### 1.1.3 其他开源多机器人地图共享项目

- 检索结论：**没有发现第二个"分布式 P2P 共享整张栅格"的开源工程**。检索到的都是：中心化合并（m-explore / DanielePS-9/MultiRobot_Mapping https://github.com/DanielePS-9/MultiRobot_Mapping 等）、稀疏特征 P2P（Swarm-SLAM https://github.com/MISTLab/Swarm-SLAM）、或单机。
- 最接近的学术工程先例仍是微机器人 stigmergic SLAM（Triet & Thinh 2026, *Int. J. Adv. Robot. Syst.*, https://journals.sagepub.com/doi/full/10.1177/17298806261432728）：**有界单调计数器 + 幂等瓦片合并 + 时变连通收敛保证 + "proximal delta encoder 只传变化 runs"**。

### 1.2 增量 + 快照的传播模式（"谁在用"，含新节点初始化）

#### 1.2.1 分布式系统/区块链（模式成熟，全部是"gossip 传播增量 + pull/快照初始化"）

| 系统 | 增量传播 | 新节点/落后节点初始化 | 链接 |
|---|---|---|---|
| **Filecoin** | GossipSub（/fil/blocks、/fil/msgs）传播新区块 | **ChainSync**：hello 协议（两节点首次相遇交换链头）+ **BlockSync**（按高度 pull 拉区块）追链 | https://spec.filecoin.io/systems/filecoin_blockchain/chainsync/ |
| **Ethereum** | devp2p 区块 gossip | **snap sync**（按状态树 range 拉取 + heal） | https://geth.ethereum.org/docs/fundamentals/sync-modes |
| **Hyperledger Fabric** | gossip 传播区块 | **state transfer**：落后 peer 向持有缺失区块的 peer **pull** | https://hyperledger-fabric.readthedocs.io/en/latest/gossip.html |
| **Raft** | AppendEntries（日志增量） | **InstallSnapshot RPC**（落后 follower 装全量快照后继续增量） | https://raft.github.io/raft.pdf |
| **OrbitDB**（libp2p 生态） | 订阅 topic 后 pubsub 广播新 log heads | **订阅时对端反向 dial（自定义 libp2p 协议）直接交换 log heads**，完成初始同步后再走 pubsub 增量 | https://api.orbitdb.org/sync.js.html |
| **ZMQ Clone / Last Value Cache** | SUB 订阅更新流 | 客户端 DEALER 请求全量状态；**文档明确警告快照/更新到达顺序竞争**（state 请求往返慢于 SUB 建连订阅时，快照可能比更新旧） | https://zguide.zeromq.org/docs/chapter5/ |

**规律**：在 P2P gossip 生态（Filecoin、OrbitDB、Fabric），新节点初始化几乎**从不**靠"pubsub 重放"，而是靠**独立的 pull/直接交换通道**。快照重放（retained/latched）是 **broker / DDS 语义**：MQTT retained（https://www.hivemq.com/blog/mqtt-essentials-part-8-retained-messages/）、ROS1 latched topic（/map 就是）、DDS **TRANSIENT_LOCAL durability QoS**（ROS2 的 latched 等价物，https://docs.ros.org/en/jazzy/Concepts/Intermediate/About-Quality-of-Service-Settings.html）。

#### 1.2.2 libp2p gossipsub：快照缓存的实际使用案例

- **gossipsub 无原生 retained/LVC**。规范只定义了：mesh + IHAVE/IWANT + 消息缓存（`mcache_len=5`、`mcache_gossip=3`、heartbeat 1s，见 https://github.com/libp2p/specs/blob/master/pubsub/gossipsub/gossipsub-v1.0.md ）。新订阅者只能靠 IHAVE/IWANT 补齐**最近约 3~5 秒**的消息，无法拿到全量状态。
- **LVC 提案**：`libp2p/research-pubsub#12` "Last Value Cache"（https://github.com/libp2p/research-pubsub/issues/12）——"解决新订阅者加入时如何知道当前状态（Orbit 聊天室初始化）"：**发布者收到新订阅者订阅事件后，重播该 topic 最后一条消息**。这正是我们 `SnapshotCache` 的设计（`swarm_events.rs` `Subscribed` 事件 → `snapshot_cache.Get(topic)` → publish）。实验实现：js-libp2p-floodsub#32。
- 结论：我们的"订阅 FULL topic → 快照重放全量"在 libp2p 圈有**提案级先例**（LVC），且与 MQTT retained/DDS TRANSIENT_LOCAL 语义一致；但在纯 P2P 实际部署中，多数项目选择了"订阅后直接 P2P 交换状态"（OrbitDB）。**可行，但必须处理 §3-2 的顺序/去重坑。**

### 1.3 有界/饱和/截断在传感器融合中的工程实践

- **OctoMap clamp**：默认 ±(2~3.5)，理由 = 可更新性（Yguel 2007）+ 压缩。我们 ±8 更宽、更稳。
- **单调 + 有界 = 无 anti-entropy 可收敛**：max-consensus（Deplano et al. 2024, https://arxiv.org/abs/2402.18719：有向图丢包下有限时间收敛）+ stigmergic 有界单调计数器，两者收敛证明的**前提都是"单调 + 幂等"**（只增不减 / max / latest-wins）。
- **加减双向（PN-counter 型）没有"无 anti-entropy"的收敛结论**：加法计数器的丢包缺口在无全量对账时**永久存在**；δ-CRDT 论文（Baquero et al., https://arxiv.org/abs/1603.01529）给收敛保证时也明确要求 anti-entropy（周期状态交换）。
- 工程界"真的只靠饱和不做反熵"的检索：**未找到**。凡是给收敛保证的系统都有反熵（周期全量/pull/InstallSnapshot）；只有依赖单调幂等合并的系统可以省掉。

### 1.4 gossipsub 用于地图/状态同步的实际用法

- 检索未发现任何机器人项目用 gossipsub 同步整张栅格地图（再次确认我们方案的独特性）。
- gossipsub 用于状态同步的通用模式（Filecoin 等）：**gossip 只做"新状态"传播，初始化/追平一律走独立 pull 协议**（见 1.2.1）。
- 本项目现状核对：`network_service.rs` 用 `gossipsub::ConfigBuilder::default()`（libp2p 0.56 / libp2p-gossipsub 0.49.5）；消息默认 **Strict 签名验证**；默认消息 ID = **(source, seqno)**（见 1.3 与 §3-5）；默认 `max_transmit_size = 65536B`（§3-1）。

---

## 2. 方案对照检查表（我们 vs 类似方案）

| 维度 | 我们的方案（定案） | 最接近的先例 | 差异 / 判定 |
|---|---|---|---|
| 地图表示 | i8 log-odds，clamp ±8，阈值 ±6 | OctoMap（i16 log-odds，clamp ±2~3.5）；m-explore（0..100 概率 + max） | 表示同族（log-odds）；**参数偏宽**，合理但需写明理由 |
| 融合规则 | 接收方累加增量（+3/-1，截断），merged 截断 | 理论 log-odds 加法（Birk&Carpin、OctoMap 多机）；m-explore 实际取 max | 我们的"加法"符合理论族；m-explore 的 max 是更简单工程取舍。**注意：加法对重复观测更敏感（过度自信）** |
| 增量传播 | gossipsub 广播增量（2~5s 打包，含饱和格） | stigmergic"只传变化 runs"；m-explore map_updates；Filecoin/OrbitDB gossip 传播 | 同族。**带宽估算不自洽（§3-3）** |
| 全量/初始化 | FULL topic ~30s 广播 + 订阅时快照重放 | m-explore 全量 map topic；MQTT retained / DDS TRANSIENT_LOCAL / ROS1 latched；Raft InstallSnapshot | **快照重放 = MQTT/DDS 语义，P2P 上仅有提案级先例（LVC）；新节点正确性有缺口（§3-2）** |
| 老节点收到全量 | 协议约定忽略 | m-explore：老节点以全量作为刷新源（反熵） | **我们浪费了已在广播的反熵机会（§3-3 修正 b）** |
| anti-entropy | **不做**，靠饱和吸收 + 信息延迟 | 无同类先例（同类收敛证明都靠单调+幂等；加法型都要反熵） | **最弱的一环（§3-3）**；是有条件的工程取舍，不是收敛保证 |
| 去重 | Network 内容哈希消息 ID | gossipsub 默认 (source,seqno) | **与 0.49 实现不符（§3-5）**；应用层需 (发送者,序号) 去重 |
| 坐标系 | 全局物理坐标（RTK），无需对齐 | Kimera-Multi/Swarm-SLAM 都做机间回环/对齐；m-explore 需已知初始位姿或特征匹配 | 我们靠 RTK 跳过对齐——成立的前提是 **RTK 持续有效**（§3-7 坏车风险） |
| 有界 | 全程有界（own ±8 / 增量截断 / merged 截断） | OctoMap clamp；max-consensus 有界单调 | 有界方向正确；但"有界 + 加法"不等于"无反熵可收敛" |
| 分 topic | FULL / DELTA 两个 topic | m-explore：map + map_updates 两个 topic | **同构，业界验证过** |
| 规模 | N≤10，空间/任务局部性 | 区块链 gossip 到上千节点 | 我们的负载远小于先例，容量无忧 |

**一句话对照**：骨架（增量 + 全量 + 有界截断 + 分 topic）在工程上有多重先例（尤其 m-explore 的 map/map_updates、ZMQ LVC、Raft InstallSnapshot、δ-CRDT）；**"不做 anti-entropy"与"快照重放的正确性"两个点没有同类先例背书，是本方案最需要补强的地方**。

---

## 3. 问题清单（按严重度排序，附证据与修正建议）

### 3.1 【P0 / 高】MAP_FULL 帧超过 gossipsub 默认 max_transmit_size（64KB），发布会失败

- **证据**：
  - rust-libp2p-gossipsub 0.49.5 源码 `config.rs`：`pub fn default_max_transmit_size() -> usize { 65536 }`，"The maximum byte size for each gossipsub RPC (default is 65536 bytes)"（https://docs.rs/crate/libp2p-gossipsub/0.49.2/source/src/config.rs ）。项目锁定的版本正是 0.49.5（Cargo.lock）。
  - 全量帧尺寸：`encode_map_full` payload = 4+4+4+2+2+4+65536 = **65556B**；`encode_frame` 帧头 = 10 + sysid(Ed25519 multihash 38B) + compid 1 + msgid 2 + checksum 2 ≈ 53B → 帧 ≈ **65609B > 65536**。
  - 任务文档 P9 写"全量 64KB << gossipsub 1MiB ✓"——**1MiB 是 go-libp2p/旧版假设，rust-libp2p 0.49 默认是 64KB**，且帧是 65536 字节数据 + 帧头，必然超限。
- **修正**：`gossipsub::ConfigBuilder` 显式 `max_transmit_size`（≥ 70KB，建议 128KB/256KB 留余量）；或在协议层压缩全量（run-length——大块 Free/Unknown 连续区压缩收益大，m-explore / stigmergic delta encoder 都这么做）；或全量分片（不推荐，复杂）。**实施第一件事。**

### 3.2 【P0 / 高】新节点初始化的正确性缺口：full/delta 顺序竞争 + full 重复应用

- **场景 A（顺序竞争）**：新节点 C 订阅 FULL + DELTA。C 可能先收到某车 A 的**缓存 delta**（t=-2s，SnapshotCache 重放 DELTA topic）或**实时 delta**，后收到 A 的 **full**（t=-30s 的 replay）。先应用 delta（基=0）再应用 full（基=覆盖到 -30s）→ **delta 的贡献被 full 覆盖丢失**（该格在 -2s 变了且之后再不变 → 永久小缺口）。这正是 ZMQ Clone 文档警告的竞争（https://stackoverflow.com/questions/36135891/zeromq-clone-pattern-and-late-joining-client ）。
- **场景 B（full 重复应用）**：C 先收到 A 的 replay-full（订阅触发），30s 后又收到 A 的**周期 full**（C 已订阅 FULL，必收）。两帧字节不同（地图演进了）→ 内容哈希/消息 ID 去重都挡不住 → **A 的贡献被加了两遍**（或旧节点再次加一遍）。
- **证据**：当前 `SnapshotCache` 对每个已订阅 topic 都重放缓存（`swarm_events.rs:389-399`），DELTA topic 的缓存 = 最近一帧 delta；任务文档 P4 只说了"订阅 FULL 重放全量初始化"，未处理与 DELTA 的次序、未处理"每个发送者恰好应用一次 full"。
- **修正（选一，建议组合）**：
  1. **DELTA topic 不重放缓存**（FULL 之外的 topic 跳过 replay）：新节点只用 FULL 初始化 + 实时 delta 跟流，去掉 stale-delta 竞争源；
  2. **帧增加 (发送者, 单调序号/epoch)**：FULL 带 per-sender epoch，新节点按 (sender, epoch) 去重（每个发送者的 full 只应用一次）；delta 带 per-sender seq，新节点丢弃"早于已应用 full 水印"的 delta（或缓存到 full 应用后再放行）——这就是上一轮报告 §4.1 要求的 CRDT 化，绕不开；
  3. 若要完全规避竞争：走 OrbitDB/Filecoin 的**独立 pull 通道**（libp2p request-response 已有现成实现 `Src/Network/Request_Response/`），新节点订阅后主动向各 peer 拉 full——顺序由本地控制，最干净，代价是协议面略增。

### 3.3 【P1 / 高】"不做 anti-entropy 只靠饱和吸收"：无同类先例，且与带宽估算不自洽

- **语义二选一导致的矛盾**：
  - 若"增量 = 仅值变化格"（带宽估算 400 格/周期成立）：发送者 own 格饱和（±8）后**不再变化、不再广播** → 接收者丢掉的中间 delta **永远补不回来**（发送者已停止发该格）→ "饱和吸收兜底"对"曾经活跃但已饱和/已离开视野"的格**不成立**。
  - 若"增量 = 含饱和格的观测更新（wb 决策4：扫到就发）"：饱和吸收成立（接收者持续 +3/-1 直到自己也饱和），但广播量 = **视野内可观测格数**而非变化格数。典型 360°/30m 雷达：视野面积 π×30² ≈ 2827 m²，0.5m 分辨率 ≈ **1.1 万格**，每格 9B → **~100KB/周期**；2~5s 周期 → **20~50KB/s/车**，比方案估算（0.7~1.8KB/s）大 10~30 倍，N=5 时每车入站 ~80~200KB/s。
- **先例裁决**：收敛证明（max-consensus、stigmergic）都靠**单调 + 幂等**；加减双向计数 + 无反熵 = **无收敛保证**（δ-CRDT 明确要求 anti-entropy）。检索未发现"只靠饱和不做反熵"的加法系统。我们 P2 的人类拍板是**工程取舍**（接受偶尔丢包影响不大），可以保留，但必须：(a) 明确残余分歧范围；(b) 修正带宽模型。
- **修正建议（低成本拿到真反熵）**：
  - FULL 帧**本来就在每 30s 广播**（老节点当前忽略）。把它变成反熵源：老节点收到 full 后，**将该发送者的累积贡献替换为其 own 表**。这需要**每发送者累积表**（own/merged 之外 N×64KB；N=5 → +320KB，仍远小于单帧 64KB 的量级，内存代价可忽略）。收益：丢包缺口在 ≤30s 内被**数学上**修复，而不是概率上；坏车可"踢表"（见 3.7）。
  - 若坚持零额外内存：保留"无反熵"，但把带宽模型改为"扫到就发 ≈ 视野格数"，并明确收敛语义 = **强最终一致 + 残余分歧有界（±clamp 内）+ 未访问格可能永久不一致**。

### 3.4 【P1 / 中】多发送者全量初始化的合并语义需要"每发送者"概念

- 问题：merged = own + Σ各车增量累积。新节点对 A、B 两个 full 做初始化时，**必须把 A 的 full 和 B 的 full 各自作为独立加法项**；若实现成"第一个 full 覆盖 merged，第二个 full 只在未覆盖格上叠加"，重叠区会错。没有每发送者记账时，**"收到 2 个 full + 中间若干 delta"无法正确重放**。
- 证据：方案核心模型里 merged 是"own + Σ增量"，但新节点没有任何 per-sender 状态；而 per-sender 状态恰是上一轮报告 §4.1 要求的 CRDT 语义。
- 修正：与 3.2 的 (发送者, 序号) 一起去重方案合并解决；实现时 merged 至少按"Σ sender 贡献"分桶（内存见 3.3）。

### 3.5 【P2 / 中】消息 ID 去重：任务文档决策7"内容哈希"与 libp2p 0.49 默认不符

- 证据：libp2p-gossipsub 0.49 文档/源码："The default value is to concatenate the source peer id with a sequence number"，且明确"content based addressing 需自行 `with_message_id_fn(hash)`"。
- 影响：同字节重发布**不会**被内容哈希过滤（决策7 的表述是错的）；但**网络层 at-least-once 重复**由 (source,seqno) 去重兜住（同一 publish 的中继副本同 msgid）。这个默认其实更适合多发送者（不同车发相同字节不会误判为同一消息）。
- 修正：改文档；不依赖内容哈希。应用层去重仍按 3.2/3.4 的 (发送者, 序号) 做（这才是增量/全量收敛所需）。

### 3.6 【P2 / 中】实施层面未定项（既有 P5~P9 的补充）

- **P5 msgid**：倾向新增 msgid（MAP_FULL 改 i8 语义 + WS 下行路径 `server.rs:146-162` 用的还是三态 state_bytes，不能简单复用）。
- **P7 数值范围**：N=5 时 merged 理论 = own(±8)+4×8 = ±40；clamp ±20 时 i8 够（±127）；若未来放宽 clamp 或 N 增大需 i16。定案前写死 clamp 常数并加断言。
- **P6 merged 读写**：consumer/executor/WS 读 merged 还是 own-only 需定案（任务已列）。
- **广播周期**：2s/5s/10s 未定——建议与"含饱和格"带宽模型（3.3）联调再定；5s 起步、必要时压缩。
- **重启车 own 表丢失**：重启后 own 归零 → merged 中自己的历史贡献消失直到重新观测。可选：own 表持久化（64KB，成本低），或接受（车辆重新走一遍即重建）。

### 3.7 【P2 / 低】坏车/RTK 降级无撤销机制

- 场景：某车 RTK 漂移/标定错误 → 该车观测写入错误的全局格 → 其 +3/-1 永久进入所有车 merged，**无 per-sender 表时无法剔除**；其他车只能靠自身观测覆盖（而 log-odds 加法下，错误的 +3 不会被正确车的 -1 完全抵消）。
- 证据：方案假设"RTK 维持全局坐标系"（决策8），但没处理"RTK 失效瞬间"与"该车贡献的撤销"。
- 修正：per-sender 记账（3.3）附带获得"剔除某车贡献"能力；或至少加"地图 epoch/整车重置"机制（上一轮报告 §4.2-1 的 map versioning 同样指向这一点）。低概率高破坏，记录即可。

### 3.8 【P3 / 低】动态障碍物 / 震荡格

- 与 P8（SEC 容忍阈值附近抖动）一致；补充：多车对同一格观测方向冲突（一车 +3 一车 −1）时，加法合并后长期卡在阈值附近 → 宏观态抖动。已定义为 SEC，规划层容忍；无需改。

---

## 4. 最终结论

### **有条件成立（需修正后实施）**

**成立的部分（有先例、方向正确）**：
- 增量广播 + 全量快照初始化 + 有界截断 + 分 topic：工程上被 m-explore（map/map_updates 双 topic）、Raft（AppendEntries/InstallSnapshot）、ZMQ LVC、MQTT retained / DDS TRANSIENT_LOCAL、δ-CRDT 反复验证过；
- 放弃"宏观三态变化广播"改"log-odds 增量"：数学上更干净，上一轮报告已确认是明显进步；
- 全程有界（own ±8 / 增量截断 / merged 截断）：方向正确，且是"重复计数过度自信"的自然解药；
- 规模（N≤10）与消息大小：负载远低于先例系统，容量无忧（除 3.1 的 64KB 上限）。

**实施前必须修正的 3 个点**（否则跑不通或正确性不成立）：
1. **gossipsub max_transmit_size 64KB 上限**（§3.1）——MAP_FULL 直接发布失败，第一件事；
2. **full/delta 顺序与去重语义**（§3.2 / §3.4）——给 FULL/DELTA 加 (发送者, 单调序号/epoch)，DELTA topic 不做快照重放，或改走现成 request-response pull 初始化；
3. **anti-entropy 决策的语义与带宽不自洽**（§3.3）——明确"增量=观测更新（含饱和格）"并重算带宽；建议**把已在广播的 30s FULL 用起来当反熵**（每发送者表，N=5 仅 +320KB 内存），把"不做反熵"从概率兜底升级为真收敛。

**可选增强**：own 表持久化（重启车不丢贡献，§3.6）；per-sender 表带来的"坏车剔除"能力（§3.7）。

一句话：**骨架成立，三处补丁（64KB 上限、full/delta 排序去重、FULL 反熵化）打完即可按计划实施。**

---

## 附：本报告引用的关键链接

- m-explore map_merge 源码：https://github.com/hrnr/m-explore （`map_merge/src/combine_grids/grid_compositor.cpp`）；ROS2 版：https://github.com/robo-friends/m-explore-ros2
- OctoMap：https://octomap.github.io ；多机 JFR/IEEE 2014：https://ieeexplore.ieee.org/document/6974556 ；Yguel 2007：https://inria.hal.science/inria-00199713/document
- gossipsub v1.0/v1.1 规范：https://github.com/libp2p/specs/blob/master/pubsub/gossipsub/gossipsub-v1.0.md ；LVC 提案：https://github.com/libp2p/research-pubsub/issues/12
- rust-libp2p gossipsub 0.49 源码（max_transmit_size、message_id 默认）：https://docs.rs/crate/libp2p-gossipsub/0.49.2/source/src/config.rs
- Filecoin ChainSync（GossipSub 传播 + BlockSync/hello 初始化）：https://spec.filecoin.io/systems/filecoin_blockchain/chainsync/
- OrbitDB sync 协议（订阅→反向 dial 交换 heads→pubsub 增量）：https://api.orbitdb.org/sync.js.html
- ZMQ Clone / Last Value Cache（含顺序竞争警告）：https://zguide.zeromq.org/docs/chapter5/
- ROS2 QoS TRANSIENT_LOCAL：https://docs.ros.org/en/jazzy/Concepts/Intermediate/About-Quality-of-Service-Settings.html
- δ-CRDT：https://arxiv.org/abs/1603.01529 ；max-consensus 丢包收敛：https://arxiv.org/abs/2402.18719
- stigmergic 微机器人 SLAM（有界单调计数器 + 幂等瓦片合并 + delta encoder）：https://journals.sagepub.com/doi/full/10.1177/17298806261432728
- Ethereum snap sync：https://geth.ethereum.org/docs/fundamentals/sync-modes ；Fabric gossip/state transfer：https://hyperledger-fabric.readthedocs.io/en/latest/gossip.html
